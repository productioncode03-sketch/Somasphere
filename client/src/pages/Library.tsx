import { useMemo, useState } from "react";
import { Download, FileText, Filter, Search, SlidersHorizontal } from "lucide-react";
import { trpc } from "@/lib/trpc";

export default function Library() {
  const { data } = trpc.content.library.useQuery();
  const [subject, setSubject] = useState("All subjects");
  const [grade, setGrade] = useState("All grades");
  const [query, setQuery] = useState("");
  const materials = (data ?? []).filter((item) => Boolean(item.fileUrl)).map((item) => ({ ...item, fileUrl: item.fileUrl ?? "", type: item.materialType === "past_paper" ? "Past paper" : "Learning material", format: item.materialType === "past_paper" ? "PDF" : "Guide" }));
  const visible = useMemo(() => materials.filter((item) => (subject === "All subjects" || item.subject === subject) && (grade === "All grades" || item.grade === grade) && item.title.toLowerCase().includes(query.toLowerCase())), [materials, subject, grade, query]);
  return <div className="mx-auto max-w-6xl"><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-[#e47d50]">Study resources</p><h1 className="mt-2 font-display text-4xl tracking-tight">The library</h1><p className="mt-3 max-w-xl text-sm leading-6 text-[#6b7f78]">Past papers and clear learning materials to help you prepare with confidence.</p></div><div className="relative"><Search className="absolute left-3 top-3 text-[#9aaca3]" size={17}/><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search resources" className="w-full rounded-xl border border-[#dfe8df] bg-white py-2.5 pl-10 pr-4 text-sm outline-none ring-[#a6c4ae] focus:ring-2 md:w-64"/></div></div><div className="mt-8 flex flex-wrap items-center gap-3 rounded-2xl border border-[#e0e9df] bg-white p-3"><div className="flex items-center gap-2 px-2 text-sm font-medium text-[#6b7f78]"><SlidersHorizontal size={16}/> Filter by</div><select value={subject} onChange={(event) => setSubject(event.target.value)} className="rounded-xl border border-[#e0e9df] bg-[#f8f7f3] px-3 py-2 text-sm text-[#173b36] outline-none"><option>All subjects</option><option>Mathematics</option><option>Science</option><option>English</option><option>Social Studies</option></select><select value={grade} onChange={(event) => setGrade(event.target.value)} className="rounded-xl border border-[#e0e9df] bg-[#f8f7f3] px-3 py-2 text-sm text-[#173b36] outline-none"><option>All grades</option><option>Grade 7</option><option>Grade 8</option><option>Grade 9</option></select><span className="ml-auto hidden items-center gap-2 text-xs text-[#7b918a] sm:flex"><Filter size={14}/> {visible.length} resources</span></div><div className="mt-6 grid gap-4 md:grid-cols-2">{visible.map((item) => <article key={item.id} className="group flex flex-col justify-between rounded-3xl border border-[#e0e9df] bg-white p-5 transition hover:-translate-y-1 hover:shadow-xl hover:shadow-[#173b36]/5"><div className="flex items-start justify-between gap-4"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-[#e5f1e6] text-[#1d5c4e]"><FileText size={20}/></div><span className="rounded-full bg-[#f3f7f2] px-3 py-1 text-[11px] font-medium text-[#6b7f78]">{item.format}</span></div><div className="mt-6"><p className="text-xs font-semibold uppercase tracking-[0.14em] text-[#e47d50]">{item.type}</p><h2 className="mt-2 font-display text-xl">{item.title}</h2><p className="mt-2 text-sm text-[#7b918a]">{item.subject} · {item.grade}</p></div><div className="mt-6 flex gap-2"><a href={item.fileUrl} target="_blank" rel="noreferrer" className="flex-1 rounded-xl bg-[#173b36] px-4 py-3 text-center text-sm font-semibold text-white hover:bg-[#24574d]">View resource</a><a href={item.fileUrl} download aria-label={`Download ${item.title}`} className="grid w-12 place-items-center rounded-xl border border-[#dfe8df] text-[#173b36] hover:bg-[#f3f7f2]"><Download size={17}/></a></div></article>)}{visible.length === 0 && <div className="col-span-full rounded-3xl border border-dashed border-[#cddbd0] bg-white p-12 text-center text-sm text-[#6b7f78]">No resources match those filters yet.</div>}</div></div>;
}
